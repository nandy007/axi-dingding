<view id="{{id}}" name="{{name}}" class="axi-rich-text {{className||''}}" style="{{style||''}}" onTap="f">
  <axi-dd-rich-text a:if="{{core==='dd'}}" nodes="{{trueNodes}}" />
  <rich-text a:else nodes="{{trueNodes}}" />
</view>